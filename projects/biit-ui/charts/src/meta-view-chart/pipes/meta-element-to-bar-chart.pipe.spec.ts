import { MetaElementToBarChartPipe } from './meta-element-to-bar-chart.pipe';

describe('MetaElementToBarChartPipe', () => {
  it('create an instance', () => {
    const pipe = new MetaElementToBarChartPipe();
    expect(pipe).toBeTruthy();
  });
});
