import { RemoveFromArrayPipe } from './remove-from-array.pipe';

describe('RemoveFromArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveFromArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
