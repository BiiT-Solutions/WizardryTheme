import { ExtractDataPipe } from './extract-data.pipe';

describe('ExtractDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractDataPipe();
    expect(pipe).toBeTruthy();
  });
});
