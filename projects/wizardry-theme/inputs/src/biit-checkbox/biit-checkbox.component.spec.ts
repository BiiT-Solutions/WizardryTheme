import {BiitCheckboxComponent} from './biit-checkbox.component';

describe('BiitCheckboxComponent', () => {
  it('should create', () => {
    const component = new BiitCheckboxComponent();

    expect(component).toBeTruthy();
  });
});

