import {BiitTooltipComponent} from './biit-tooltip.component';

describe('BiitTooltipComponent', () => {
  it('should create', () => {
    const component = new BiitTooltipComponent();

    expect(component).toBeTruthy();
  });
});

