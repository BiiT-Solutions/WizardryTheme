import {BiitNotificationComponent} from './biit-notification.component';

describe('BiitNotificationComponent', () => {
  it('should create', () => {
    const component = new BiitNotificationComponent();

    expect(component).toBeTruthy();
  });
});

