import {LineChartComponent} from './line-chart.component';

describe('LineChartComponent', () => {
  it('should create', () => {
    const component = new LineChartComponent();

    expect(component).toBeTruthy();
  });
});

