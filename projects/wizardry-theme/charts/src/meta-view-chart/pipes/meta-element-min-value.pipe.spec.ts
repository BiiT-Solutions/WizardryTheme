import { MetaElementMinValuePipe } from './meta-element-min-value.pipe';

describe('MetaElementMinValuePipe', () => {
  it('create an instance', () => {
    const pipe = new MetaElementMinValuePipe();
    expect(pipe).toBeTruthy();
  });
});
