import { MetaElementMaxValuePipe } from './meta-element-max-value.pipe';

describe('MetaElementMaxValuePipe', () => {
  it('create an instance', () => {
    const pipe = new MetaElementMaxValuePipe();
    expect(pipe).toBeTruthy();
  });
});
